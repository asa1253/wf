# Warframe 精通进度追踪 ⚔️

用 **GitHub Actions 定时抓取** + **GitHub Pages 自动发布**，把你的 Warframe 精通进度做成一个可搜索、带进度条的静态网页。

## 功能
- ✅ 读取 DE 公开档案 + warframestat.us 物品库，自动解析段位、总精通经验
- ✅ 进度条：段位升级进度、分项完成度、每件物品满级进度
- ✅ 物品清单表格：搜索、分类筛选、状态筛选、点击表头排序
- ✅ GitHub Actions 每天定时自动更新，也可手动触发
- ✅ 自动部署 GitHub Pages

## 目录结构
```
warframe-mastery/
├── .github/workflows/update.yml   # 定时抓取 + 部署
├── scripts/fetch_mastery.py       # 数据抓取 / 解析 / 渲染
├── templates/index.html           # 页面模板
├── data/                          # 生成的数据（自动覆盖）
└── index.html                     # 生成的成品页面（自动覆盖）
```

## 快速开始
1. **登录官网**并获取 `user_id`：
   - 打开 https://www.warframe.com/login 登录
   - 打开 https://www.warframe.com/api/user-data ，复制 `user_id` 字段值
2. 把 `scripts/fetch_mastery.py` 顶部的 `USER_ID` 替换成你的值（也可用 `--user-id` 传入）
3. 把本目录推送到你的 **GitHub 仓库**
4. 仓库 `Settings → Pages → Source` 选择 **GitHub Actions**
5. 运行：
   - 自动：每天 UTC 08:00 定时更新
   - 手动：`Actions → Update & Deploy Mastery Page → Run workflow`

## 本地运行
```bash
# 在线模式（需要真实 user_id）
python scripts/fetch_mastery.py

# 演示模式：离线验证生成管线（内置样例数据）
python scripts/fetch_mastery.py --demo
```
生成 `index.html` 后用浏览器打开即可预览。

## 依赖与说明
- Python 依赖：`requests`、`jinja2`
- 玩家档案必须在游戏内**设置为公开**，否则个人接口返回空/登录页
- 精通经验每等级数值与段位门槛表依据 **Warframe Wiki**，位于脚本顶部常量 `MASTERY_XP_PER_RANK` 与 `MR_THRESHOLDS`，可按实际核对调整
- 原始抓取数据会保存到 `data/raw_profile.json`，若 DE 接口字段变动，可对照它调整 `parse_profile()` 的字段映射

## 扩展方向
- 按武器/战甲类型过滤、统计特定来源（如 Prime、Kuva）
- 接入 DE 任务接口展示每日突击/钢铁之路奖励
- 多账号对比页
